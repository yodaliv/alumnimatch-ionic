import { MatchOrderPipe } from './match-order.pipe';

describe('MatchOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
