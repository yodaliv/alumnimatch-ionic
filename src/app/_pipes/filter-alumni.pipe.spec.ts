import { FilterAlumniPipe } from './filter-alumni.pipe';

describe('FilterAlumniPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAlumniPipe();
    expect(pipe).toBeTruthy();
  });
});
